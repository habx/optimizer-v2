numpy==1.16.4
shapely==1.6.4.post2
# We need to build ortools from source. Check Dockerfile.pypy to see how
#ortools==7.1.6720
Dijkstar==2.4.0
networkx==2.2

# WARNING: This should not be shared externally. We're using a token because fetching private
#          dependencies using the SSH key prior to building the container is a lot more difficult,
#          mostly because protobuf install doesn't respect the virtualenv.
git+https://habxtech:57cd73fc9c2e8535cf897e45538a13eb4bc9017b@github.com/habx/lib-py-logger.git@v1.2.0#egg=habx_logger==v1.2.0

# For code analysis:
pprofile
pyinstrument

# For the worker:
boto3
requests

# For tests and linting:
pytest
pylint==2.2.2

# Needed for ortools
cppyy==1.4.11
cppyy-backend==1.8.4
cppyy-cling==6.15.2.9